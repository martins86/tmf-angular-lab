import { Routes } from '@angular/router';

import { FontSizeComponent } from '@pages/font-size/font-size.component';

export const FONT_SIZE_ROUTE: Routes = [
	{
		path: '',
		component: FontSizeComponent,
	},
];
